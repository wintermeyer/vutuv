# App artifacts
/_build
/db
/deps
/*.ez

# Generate on crash by the VM
erl_crash.dump

# Static artifacts
/node_modules

# Since we are building assets from web/static,
# we ignore priv/static. You may want to comment
# this depending on your deployment strategy.
/priv/static/

# The consent blocker the screenshot browser injects: verbatim copies of
# @duckduckgo/autoconsent's prebuilt bundle and rule set, placed here by
# `mix assets.setup` (see the alias in mix.exs). Generated, not authored.
/priv/chrome/autoconsent/autoconsent.js
/priv/chrome/autoconsent/rules.json

# The config/prod.secret.exs file by default contains sensitive
# data and you should not commit it into version control.
#
# Alternatively, you may comment the line below and commit the
# secrets file as long as you replace its contents by environment
# variables.
/config/*.secret.exs
/secrets/

# attachments in local storage (avatars, covers, post images + url
# screenshots, plus the private originals tree); in production these live
# under config :vutuv, :uploads_dir_prefix (/srv/legacy-vutuv)
#
/avatars
/covers
/post_images
/review_covers
/screenshots
/originals
/moderation_evidence
/qualification_documents
/organization_images
/job_posting_images
# pictures fetched from other networks (Vutuv.RemoteMedia): a followed
# account's avatar and its posts' attachments
/remote_media
# the AI-moderation limbo twin of the served trees (Vutuv.Uploads.quarantine_dir/1)
/quarantine

# OS X stuff
#
.DS_Store

# Misc
#
log.csv

# Local planning docs (not for version control)
#
/docs/plans/

# Distillery
#
rel/vutuv

# Sublime configuration
#
vutuv.sublime-project
vutuv.sublime-workspace

assets/node_modules
.vscode/*

# Codex (at least for now)
.codex/*

# Recovered invitation recipient addresses (issue #942 reseed). Contains PII —
# NEVER commit. Fed once to Vutuv.Release.reseed_invitations/2, then deleted
# from the working tree and the server.
/invited_emails.local.txt
*.local.txt

# Transient log written by the deploy skill's `mix precommit` run (redirected
# to a file so its output never streams into the agent's context). Disposable.
/precommit.log

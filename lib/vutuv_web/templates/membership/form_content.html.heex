<.form :let={f} for={@changeset} action={@action} class="editform">
  <.form_error changeset={@changeset} />

  <div class={"editform__field#{if error_tag(f, :group_id), do: " editform__field--error"}"}>
    <%= label f, :group_id, class: "control-label" %>
    <%= text_input f, :group_id, class: "form-control" %>
    <%= error_tag f, :group_id %>
  </div>

  <.form_actions backlink={@backlink} />
</.form>

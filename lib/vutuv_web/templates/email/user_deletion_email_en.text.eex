<%= email_greeting(@user) %>,

please use the following PIN to delete your account:

  <%= @pin %>

WARNING: There is no turning back from this move. Your account will be
deleted as soon as you enter this PIN on vutuv. The PIN expires after
30 minutes.

But of course you can always create a new account.

<%= render "_signature_en.text" %>

<%= render "_footer.text" %>

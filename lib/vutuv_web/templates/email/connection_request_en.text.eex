<%= email_greeting(@user) %>,

@<%= @actor_slug %> wants to connect with you on vutuv.

Accept or decline on their profile:

<%= @url %><%= @actor_slug %>

You can switch these notification emails off here:

<%= @unsubscribe_url %>

<%= render "_signature_en.text" %>

<%= render "_footer.text" %>

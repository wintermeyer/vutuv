<.page_header
  title={gettext("Emails belonging to ") <> full_name(@user)}
  crumbs={[
    gettext("Users"),
    {full_name(@user), ~p"/#{@user}"},
    gettext("Emails")
  ]}
/>


<.card_section
  empty={Enum.empty?(@emails)}
  add_href={same_user?(@user, @current_user) && ~p"/#{@current_user}/emails/new"}
>
  <%= render "card_list.html", assigns %>
</.card_section>

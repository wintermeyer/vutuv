---
paths:
  - "**/*.heex"
  - "assets/css/**"
  - "lib/vutuv_web/**"
---

## vutuv visual design system — "Direction A"

Every page must use this one visual language. It is "confident professional-bold":
deep heritage **blue** + a **coral** accent, white cards on a light grey canvas,
clean typography. **Dark mode follows the system** (`prefers-color-scheme`) — there
is **no theme toggle**, and every surface/text needs `dark:` variants.

### Two tracks — know which one you're touching

1. **Legacy controller+view pages** still use shared classes (`.card` / `.card-list`,
   `.editform` + inputs (`.editform__hint` is the muted helper line under a field, e.g.
   "Markdown is supported"; its `--ok` brand-blue / `--error` red variants color the live
   username-availability verdict that app.js writes under the new-username field),
   `.button` + variants, `.breadcrumbs`, `.profile-header` page
   title, `.pure-table`, `.alert`, `.tags`/`.badges`, `.imagebox`,
   `.profiles` (+ the `.profiles--tags` variant whose rows grow to fit a
   `.profile-tags` line: up to 4 small tag links plus a `.profile-tags__more`
   "+N more tags" overflow count, used by the most-followed listing via
   `card_list`), `.job`, `ol.tags`/`.upvote`, `ul.thumbs` (flex rows: capped
   thumbnail + truncating URL), `.reorder*` (the owner's drag-and-drop ordering
   tool shared by every orderable section page — links, phone numbers, addresses,
   social media accounts, emails: `.reorder__item` draggable tile =
   `.reorder__handle` grip + `.reorder__body` (section-specific, with a
   `.reorder__thumb` for links) + `.reorder__move` up/down `.reorder__btn` arrows
   (sized as touch targets) + `<.row_actions>`. It is **not** a classic page: it is
   the embedded `VutuvWeb.SectionReorderLive` (`live_render`, owner-only, like the
   shell), so both drag and arrows persist over the socket with no reload — the
   `Reorder` hook in `app.js` does the drag, the arrows are `phx-click`. The hook
   also FLIP-animates the arrow reorders (snapshot row tops in `beforeUpdate`,
   glide them from old to new spot in `updated`, ~180ms; honors
   prefers-reduced-motion). Mobile-first: touch can't fire native drag, so the
   arrows are the reorder path on phones), `.card__tablewrap` (every in-card `<table>`
   sits in this overflow-x scroller so a wide table scrolls instead of being
   clipped by the card's `overflow:hidden` — `card_table_scroll_test.exs`
   enforces it), `.breakwrap` (break a long unbroken string and cap the line at a
   readable measure — use it instead of an inline `word-wrap`/`max-width` style),
   `.card__empty` empty-state line, `.card__label` (the small uppercase muted
   caption for field/section labels inside a legacy card — e.g. the detail show
   pages and the tag card; it is `<h2 class="card__label">` so each page keeps
   exactly one `<h1>` while the prominent `.card h2` stays for `.job__title`),
   `.error-page` 404/403/500
   card). They
   are styled **centrally** in `assets/css/components.css` — since the old vendored
   `legacy.css` was removed, that file is the **single source** for these classes,
   including element defaults (body canvas, h1/p/a, label, tables, dl) and the data-URI
   icons. **To restyle legacy pages, edit `components.css` — do NOT reskin
   per-template.** A new legacy page that reuses these classes gets the look for free.
   **Dark mode for all of it lives in the `@media (prefers-color-scheme: dark)` block
   at the end of `components.css`** — when you add a light rule with a hardcoded
   colour, add its dark counterpart there. `test/vutuv_web/dark_mode_css_test.exs`
   guards the canvas rule and that `legacy.css` stays deleted.
2. **New / hand-written pages** (the shell `ShellLive`, the LiveViews, `user/show.html.heex`,
   the logged-out auth/welcome pages via `<.auth_layout>`)
   use the **`VutuvWeb.UI` components** (see **Components** below) or, where no component
   fits, the **recipes** below. Prefer a component; fall back to a recipe. Reach for a
   green-field rewrite only when a page's UX needs rethinking (like the profile) —
   otherwise let track 1 handle it.

### Tokens (defined in `assets/css/app.css` `@theme`)

- **Brand blue:** `bg-brand-600` (primary), hover `bg-brand-700`; text `text-brand-700` / `text-brand-800`; tint `bg-brand-50`. The scale is **sparse** — only these steps exist in `@theme`: `50, 100, 200, 400, 500, 600, 700, 800, 900` (**no `300`, no `950`**). A `brand-300`/`brand-950` class silently compiles to nothing, so pick a defined step. After adding/changing utility classes in a `.ex`/`.heex`, rebuild (`mix tailwind vutuv`) and confirm each shipped by grepping `priv/static/assets/app.css` for the **escaped** selector (variants escape `:` to `\:`, e.g. `grep -F 'dark\:hover\:bg-brand-900'`).
- **Accent (unread counts / CTAs highlight):** `bg-accent` / `text-accent` (coral `#f97362`).
- **Surfaces:** page is grey (`slate-50`/`slate-100`); cards `bg-white`; borders `ring-slate-200`; text `text-slate-900 / 700 / 500`. **Muted/secondary text is `text-slate-600 dark:text-slate-400`** — passes WCAG AA on both white cards and the slate canvas. **Don't use bare `text-slate-400` as a text colour**: it fails AA (~2.6:1 on white) and is gone from the templates; `slate-400` now lives on only as the `dark:` half of the muted pair. Even `slate-500` is borderline directly on the slate-100 canvas (breadcrumbs, footer), so those use `slate-600`. A WCAG axe-core sweep keeps the key pages clean.
- **Dark:** `dark:bg-slate-900` (cards) / `slate-950` (page), `dark:ring-slate-800`, `dark:text-slate-100 / 300 / 400`. `app.css` sets `html { color-scheme: light dark }` and `root.html.heex` ships a `theme-color` meta per scheme (`#ffffff` / `#020617`).

### Canonical recipes (copy these exactly)

- **Page root:** `<main>` already gives `mx-auto max-w-6xl px-4`. A page's own wrapper is e.g. `py-6` (+ `grid gap-6 md:grid-cols-3` for a content+rail layout — `md`, not `lg`, so portrait iPads get the columns too). Don't add another `max-w`/`px` unless intentional. **Give the main content column `min-w-0`** (e.g. `min-w-0 space-y-4 md:col-span-2`): Tailwind's `md:grid-cols-*` compiles to safe `minmax(0,1fr)` tracks, but only at `md+`; below `md` (every phone) the grid falls back to an implicit `auto` track that respects the item's min-content, so a single `truncate`/unbreakable descendant (a threaded reply's parent-excerpt, a markdown body) forces the column — and the whole page — wider than the viewport and scrolls it sideways. `min-w-0` lets the column shrink below that min-content. `mobile_overflow_test.exs` guards the feed / profile / dev-doc columns.
- **Card:** `rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 dark:bg-slate-900 dark:ring-slate-800` — or `<.card>`.
- **Section title:** `text-sm font-semibold uppercase tracking-wide text-slate-500` — or `<.section_title>`.
- **Primary button:** `rounded-lg bg-brand-600 px-4 py-2 text-sm font-semibold text-white hover:bg-brand-700`.
- **Secondary button:** `rounded-lg bg-slate-100 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-200 dark:bg-slate-800 dark:text-slate-200 dark:hover:bg-slate-700`. The `dark:hover:` is required: without it the dark `slate-800` flips to the light `slate-200` on hover.
- **Text link / "Add" action:** `text-sm font-semibold text-brand-600 hover:text-brand-700`.
- **Input / select / textarea:** `w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm focus:border-brand-500 focus:outline-none dark:border-slate-700 dark:bg-slate-800 dark:text-slate-100`. This exact string is `VutuvWeb.UI.input_class/0` (imported everywhere) — call it in HEEx as `class={input_class()}` (compose with utilities via `class={[input_class(), "resize-y"]}`) on green-field forms (the post composer, the auth pages) instead of re-typing it.
- **Checkbox (consent/opt-in box):** `mt-0.5 h-4 w-4 rounded border-slate-300 text-brand-600 focus:ring-brand-500 dark:border-slate-600 dark:bg-slate-800` — the string is `VutuvWeb.UI.checkbox_class/0` (the companion to `input_class/0`); used by the sign-up form's consent checkboxes (email visibility, search engines, AI agents). Call it instead of re-typing.
- **Tag chip:** `inline-flex items-center gap-2 rounded-lg bg-brand-50 px-3 py-1.5 text-sm font-medium text-brand-700 dark:bg-brand-900/40 dark:text-brand-100` — or `<.chip>`.
- **Unread count badge:** small `rounded-full bg-accent px-1 text-[11px] font-bold text-white`; add `ring-2 ring-white dark:ring-slate-900` when it overlaps an icon or avatar.
- **Avatar:** `Vutuv.Avatar.display_url(user, :medium|:thumb)` in an `<img>`, `rounded-2xl` (tile) or `rounded-full` (list) + `object-cover`.
- **Link preview card** (profile Links section, main column): `grid gap-4 sm:grid-cols-2 sm:gap-x-18` (the `sm:gap-x-18` 72px desktop column gutter lines the two thumbnail columns up edge-for-edge with the Followers/Following cards below, whose content channel is `gap-6` + two `p-6` = 72px; row/mobile gap stays `gap-4`), each cell an `<a>` wrapping `<img src={Vutuv.Screenshot.url({url.screenshot, url}, :thumb)} width="400" height="264" loading="lazy" class="aspect-[400/264] w-full object-cover">` inside `overflow-hidden rounded-lg ring-1 ring-slate-200 dark:ring-slate-800`, then a `min-w-0` title/URL block (`truncate`). The screenshots come from `Vutuv.PageScreenshot` (headless Chromium, on link create/update); `Screenshot.url/2` falls back to `/images/screenshot.png`.

### Components (`VutuvWeb.UI` — imported everywhere; no explicit import needed)

`lib/vutuv_web/templates/user/show.html.heex` is the canonical example of the kit.

- `<.card class="…">…</.card>` — the card surface.
- `<.auth_layout title=… subtitle=…>…</.auth_layout>` — the logged-out **welcome/auth shell**: a brand-gradient hero panel (`from-brand-700 to-brand-500`, soft white decorative rings, white headline + `subtitle` + optional `:hero` slot for a member-count pill) beside a `<.card>` form (the default slot), stacking to one column under `md`. A `:headline` slot replaces the default `<h1>{title}` + subtitle when a hero needs full control over its heading typography (the landing page's founder-quote + attribution block); `title` stays required as the plain-text fallback. Used by the sign-up landing (`page/index`), `/login` and the two PIN screens so the logged-out entry flow matches the app instead of the old full-bleed photo "imagebox" (now deleted). The legal pages (impressum, datenschutz) are plain `<.page_header>` + `<.card>` content (no hero).
- `<.pin_actions />` — the two "stuck on the PIN page" escape hatches shared by both PIN-entry screens (login `session/pin_user_login` + post-registration `page/pin_new_registration`): a "Resend PIN" and a "Use a different email address" text-link, each a CSRF POST (to `~p"/login/resend"` / `~p"/login/cancel"`). Resend re-mails the PIN for the pending cookie identity (rate limited 5/hour); cancel drops the identity cookie so the landing page is no longer pinned to the PIN form. Uses the standard text-link recipe; no new tokens.
- `<.section_title>…</.section_title>` — uppercase muted heading inside a card.
- `<.other_formats_card base_path={…} locale={…} vcard />` — the quiet rail/footer card of small chips linking to a page's `VutuvWeb.AgentDocs` siblings (Markdown / plain text / JSON / XML, plus vCard when `vcard` is set). `base_path` is the page's extension-free path, so each chip is `base_path <> ".md"` etc.; `locale` keeps a `?lang=` suffix on the agent links (the vCard skips it). Three call sites: the **profile** aside (`base_path={"/" <> @user.username} vcard`), the **`/feed` rail** (`base_path="/feed"`, rendered twice — desktop rail + an `md:hidden` bottom copy, so pass distinct `id`s through the global `rest`), and the **post permalink** (`base_path={@formats_base}`, gated on `@show_formats?` so a restricted post — whose anonymous `.md` would 404 — shows none). No vCard except the profile.
- **Unified card UX (showcase + onboarding):** the profile is a **showcase**, not an editor. The dashed `<.empty_add>` tile is **onboarding scaffolding** — it shows only while a card is **empty** (so a brand-new owner sees an obvious way to start filling each section). Once a card has entries it is a **clean showcase** that reads like the visitor's view, with a quiet `<.card_footer_link>` "Verwalten ›" (→ the management page, where adding more / editing / deleting happens) or, for General Info, "Ändern ›" (→ `/edit`). There is no header "Add" button and no hidden ⋯ menu; existing entries are edited/removed with the calm `<.row_actions>` on the management pages. **`Beiträge` (Posts) is the one exception:** its compose tile stays pinned to the top of the card *always* (the Twitter/Facebook compose-on-top convention, since posting is the primary, frequent action).
- `<.section_header title={…}>` — card header row: section title + an optional right-aligned `:action` slot. Under the unified card UX the owner's add is the body `<.empty_add>` tile (empty cards only), not a header button, so most call sites pass just `title`; the `:action` slot stays for the rare non-add header control.
- `<.settings_nav user={…} active={:profile|:privacy|:notifications|:apps|:account} />` — the underline-tab sub-navigation shared by the **profile editor** (`/:slug/edit`, tab `:profile`) and the **settings pages** (`VutuvWeb.SettingsController`: `/:slug/settings/privacy`, `/:slug/settings/notifications`, `/:slug/settings/apps`, and `/:slug/settings` account hub `:account`). The old everything-on-one-page edit form was split into these focused, owner-only Track-2 pages; **each page's `h1` names its own tab** (Profile / Privacy / Notifications / Apps / Account), not a shared "Settings", so a deep-landing member can tell where they are (set the literal per template; do not derive it from the nav). Each settings/edit action also sets its own `:page_title` (`Edit profile` / `Privacy settings` / `Notification settings` / `Apps & API` / `Account settings`) so the browser `<title>` stops falling back to the bare member name. The nav is how a member moves between them. **What lives where** (clustered by what a human comes to do): **Profile** = public content only (photos, name, about you); **Privacy** = a *Search engines & AI* card (the search-engine/AI consent **checkboxes** — `checkbox_class()`, reusing the sign-up form's inverted `checked_value: "false"` trick for the opt-out `noindex?`/`noai?` so the box reads positively, "Allow …"; helper line forced `font-normal` because the wrapping `<label>` defaults to bold in `components.css`) **plus** a *Safety* card with two rows, `/blocks` (blocked members) and `/moderation/cases` (content under review) — both moved here from the account hub, where members did not look for them; **Notifications** = an *Email notifications* card with one positive-flag checkbox **per type** (`notification_emails?` unread messages — default on; `email_on_connection_request?` / `email_on_endorsement?` / `email_on_follower?` — opt-in, default **off**), each wired to a real Emailer notice (see the notification-email note below), **plus** a read-only *In-app notifications* explainer linking to `/notifications`; **Apps** = the *Apps & API access* card (connected apps + access tokens + a "Read the API docs" cross-link to `/developers`), split off onto its own tab; **Account** = *Sign-in & identity* (username, emails), *Language & region* (the interface-language `locale` select — a small form posting `PUT|PATCH /:slug/settings/language` → `update_language`, kept off the profile editor since it sets the whole UI, not the public profile), *Your data* (export), and the red *Delete account* danger card. The dissolved "Privacy & security" grab-bag card is gone. Account-hub / Apps / Safety rows render through `<.account_row>` (in `SettingsHTML`). The ad banner is suppressed on every `/:slug/edit` and `/:slug/settings/*` page (`VutuvWeb.Plug.AdBanner`).
- `<.card_menu id="…"><:item href={…}>…</:item></.card_menu>` — **Track 2** per-card ⋯ dropdown (native `<details data-menu>`; `app.js` closes it on outside click and Escape). **No longer used on the profile sections** — they switched to the dashed `<.empty_add>` tile + `<.manage_footer>` (the quiet ⋯ failed non-technical owners; see the unified card UX note above). Two homes now: the **post card's author menu** (`VutuvWeb.PostComponents`): Edit + a confirmed red Delete; and the **profile header's** non-owner ⋯ menu (`VutuvWeb.UserProfileLive`), which is a LiveView. So `:item` supports **both** a navigation/CSRF link (`href` + optional `method`) **and** a LiveView action (`click` = phx-click event name, `value` = phx-value-id) — one menu mixes them: the profile's Message/Report stay `href`, while Mute/Bookmark/Like/Block are `click` items (`toggle_mute`/`bookmark_user`/`unbookmark_user`/`like_user`/`unlike_user`/`block_user`), so they toggle with no reload. `:item` also takes `confirm` (data-confirm) and `danger` (red). The owner guard sits on the slot at the call site.
- `<.empty_add href={~p"/…/new"} class={…}>{gettext("Add …")}</.empty_add>` — the owner's **add tile** / onboarding scaffold: a full-width dashed-outline tile (plus glyph + label, brand-tint hover, `data-empty-add` hook) linking to the new-entry form. On the **profile** it shows only while the card is **empty** (`:if={same_user?(…) and <collection empty>}`); once there are entries the card is a clean showcase with a `<.card_footer_link>` "Verwalten ›" instead. The label is a clear per-section call to action (`gettext("Add a link")`, `Add work experience`, `Add a tag` → `/tags/new`). The two exceptions: **Beiträge** keeps its compose tile always (`Write your first post` empty / `Write a post` populated → `/feed`); **General Info** has no list, so its empty tile (→ `/edit`) graduates to an "Ändern ›" footer once set. On the **management pages** `<.card_section>` still renders the tile above the list (empty *or* populated), since those are the editor; a **visitor's** empty management index shows the plain `card__empty` line instead. **`href` is optional:** omit it and the tile renders a `<button>` instead of a link, driving a LiveView action through the global `rest` (`phx-click` + `id`) — this is how the **`/feed` composer reveal** reuses the *same* dashed tile (label "Write a post") as a collapsed trigger that expands the inline composer, so the compose affordance reads identically on the feed and the profile.
- `<.row_actions edit_to=… delete_to=… confirm=… align={:end|:start} class=… />` — the calm, labeled per-entry **Edit/Delete** actions on every management list and entry show page: a brand "Edit" text link + a muted-red "Delete" text link that deletes through the `delete` method (CSRF) behind a `data-confirm` prompt (defaults to "Are you sure?"). The unified replacement for the loud `<.edit_delete_actions>` pencil + red-trash-circle pair on user-facing pages; omit `edit_to` for delete-only rows (tags). `align` defaults to `:end` (right-aligned, for table-row cells); pass `:start` to left-align it (the work-experience timeline puts the controls under each role). Keep the owner guard at the call site (`:if={@as_owner?}` on the section pages, `:if={same_user?(…)}` elsewhere).
- `<.card_footer_link href={~p"/…"}>{…}</.card_footer_link>` — the quiet card **footer link** for "see more / manage" navigation below a card's entries (the profile's Posts/Followers/Following "View all" links and, via `<.manage_footer>`, the section cards). A **centered, muted** text link (slate → brand on hover) with a hairline top divider and a trailing chevron, so it reads as a calm footer subordinate to the prominent dashed `<.empty_add>` tile up top — pairing the styled tile with a bare brand link looked like two design vocabularies. Guard rendering with `:if` at the call site.
- `<.manage_footer href={~p"/…"} total={…} preview={…} owner={…} />` — the owner's visible bridge from a profile section card to its management page, folded with the public "View all" link: a `<.card_footer_link>` reading "View All (N)" once `total > preview` (everyone), else "Manage" for the owner when `total >= 1`, else nothing. Replaced the old "View All" links + the ⋯ menu's "Manage entries" so removing the menu doesn't strand the owner with no way to edit existing entries.
- `<.view_as_switcher base_path={…} preview_as={…} class={…} />` — the owner-only **"View as" preview switcher** shared by the profile (`/:slug`) and every profile **section index page** (`/:slug/work_experiences`, `/phone_numbers`, `/addresses`, `/links`, `/social_media_accounts`, `/emails`, `/tags`). A segmented control (You / Public) that reloads the current page with `?view_as=<mode>`, plus an active-mode banner. (Two earlier tiers were dropped: "Follower" after the follow-only simplification (it rendered like "Public"), then "Connected"/Vernetzt once private emails became owner-only and it no longer revealed anything extra. A stale `?view_as=follower`/`?view_as=connection` link now resolves to the owner's own view.) **Rendered once, from the `app` layout** (`layout/app.html.heex`) — pinned at the top of `<main>` right below the top nav on every owner page (do not re-embed it in a template). Its width follows the page's content so it lines up with what's below it: the **profile** spans the full main column (`max-w-6xl`, the controller sets `@view_as_full_width?`), the **section pages** center at `max-w-3xl` (matching `.card-list` etc.). The layout gates it on `@can_preview?` and derives `base_path` from `conn.request_path`. The flags come from **`VutuvWeb.ViewAs.assign_preview/1`** (section controllers' `index`) or `VutuvWeb.UserController.show` (profile); templates gate every Add / Edit / Delete affordance on `@as_owner?` (owner **and** not previewing) instead of `same_user?/2`, so the Public preview renders read-only. Most sections are fully public, so the Public preview only toggles the owner's chrome there; `/emails` also drops its private addresses — a private address is **owner-only** now, so it never shows in any preview (`UserHelpers.emails_for_preview/3` returns public-only). The segments truncate and the control drops full-width below the label under `sm` so a long localized label (German "Öffentlich") still fits a narrow phone. The **Public** banner says search engines see the page; when the member has turned off indexing or AI use (`noindex?`/`noai?`) the layout passes `public_restricted?` + `settings_path` and the banner appends a "More about this." link to `/:slug/settings/privacy`, since the blanket claim no longer fully applies to them. **`live?`** (default false; the layout passes `live?={!assigns[:conn]}`): on the **profile** (a LiveView, no `@conn`) each segment is a `phx-click="view_as"` button (`phx-value-mode` = `you|public`) handled by `UserProfileLive`, so switching tiers re-renders the page server-side with **no reload** (a direct `?view_as=` URL still works — the controller reads it on full load); the dead **section pages** (`@conn` present) keep the `?view_as=` links + reload.
- `<.button>` — `primary` (default) or `variant="secondary|ghost|danger"`. Renders a link when given `navigate`/`patch`/`href` (add `method="post|delete"` for CSRF actions), else a `<button>` (set `type="submit"`). e.g. `<.button href={~p"/follows?#{…}"} method="post">Follow</.button>`.
- `<.follow_button variant="icon|text|button|segment" follower_id={…} followee_id={…} follow_id={…} />` — the **single owner of the two `~p"/follows…"` route shapes** (CSRF DELETE `/follows/<id>` to unfollow, POST `/follows?follow[…]` to follow, branched on `follow_id` — `nil`/`false` = not following). Following is **the** social action and is one-directional; a **mutual** follow is what the app calls "vernetzt" (connected) — there is no separate connect control any more. The following-state lookup (`following_by_id` map or `user_follows_user?/2`) stays at the call site; the component just consumes its result. **Four** visual variants reproduce the follow controls: `icon` = `button button--icon` + `i.icon.icon--follow|--unfollow` glyphs (the tag-endorsers table, the one compact icon-only follow control left; icon-only, so it carries a `title` + `aria-label` accessible name — "Follow"/"Unfollow" — like `<.mute_button>`), `text` = a small `rounded-full` **pill toggle** that reads as one control in two states (the `user_row` rail **and** the `card_list` listing rows — followers / following / most-followed / tag page — where it replaced the bare icon so each row labels the action in words; in `card_list` the pill sits in a right-aligned `<span>` action cluster, wrapped in an inner `<span class="flex">` so its `self-start` doesn't float it above the centered legacy `.profiles li` row): the brand-outline call-to-action "Follow" while you do not follow, a calm slate "Following" pill once you do that turns rose and swaps its label to "Unfollow" on hover/focus (CSS `group-hover`, no JS — `following_label/1`); both states share a fixed `min-w-[5.5rem]` + `justify-center` so the two pills are the **same width** (sized to fit the widest label across en/de — "Following"/"Entfolgen" ≈ 81px) and line up; outlined with a real `border` (not `ring-1`) because a box-shadow ring on a `rounded-full` pill at a sub-pixel position anti-aliases unevenly on the curved corners — a border hugs the radius crisply; deliberately brand/slate, not the header pill's reserved emerald "✓", `button` = a secondary/primary `<.button>` "Following"/"Follow" (the post-teaser, which renders only the follow half, pass `follow_id={nil}`), `segment` = the flush, square-cornered, `flex-1` `<.link>` that forms the **clickable left half** of the profile header's `<.follow_relationship>` pill (see below; not used on its own) — its colour encodes your follow state: green `bg-emerald-700` "✓ Following" once you follow, the brand call-to-action "Follow" while you do not. It carries a `transition-colors` + `active:` darken press feedback because follow/unfollow no longer flash a success toast (the `FollowController` create/delete success paths set no flash) — the button's own state change is the confirmation. `icon`/`text` render through `button/2` (a `<button>` with `data-method`); `button`/`segment` render an `<a>` via `<.button>` / `<.link>`. Keep the owner / visitor / logged-in guard on a `:if` at the call site. The profile header no longer calls `button` directly: it renders its two follow directions through `<.follow_relationship>`. **`live?`** (default false, honored by the `segment` **and** `text` variants): the profile is now a LiveView (`VutuvWeb.UserProfileLive`), so it passes `live?` and those variants fire `phx-click="follow"` (`phx-value-followee`) / `phx-click="unfollow"` (`phx-value-id`) instead of the CSRF link, toggling with no reload — the header pill (`segment`) and the follower/following/who-to-follow `user_row` rows (`text`, via `<.user_row live?>`). Every other (dead-page) call site keeps the CSRF link.
- `<.follow_relationship follower_id={…} followee_id={…} follow_id={…} follows_viewer?={…} />` — the **profile header's segmented follow control**: a **horizontal** pill at every width that **fixed width at every size: `w-52` on a phone, `w-80` on `sm+`** (`flex w-52 text-xs sm:w-80 sm:text-sm`). The phone width is fixed (not `w-auto`) on purpose: a shrink-to-fit pill mis-sized the `flex-1` halves ~1-2px under the wider label and clipped it; a fixed width gives the equal halves a stable, sufficient size. It stays a short **one-row control in the white area beside the avatar**, below the cover, instead of riding up into the cover banner. The seam shows at **every** width (it was hidden on phones before); each half **clips its own content** (`overflow-hidden` on the half, `whitespace-nowrap` label) — deliberately **no `truncate`/ellipsis**, because `text-overflow: ellipsis` fired on sub-pixel rounding and ate whole characters ("Folge i…") even when the label fit; an over-long label (the long German "Folgt dir nicht") is hard-clipped at the half/divider edge on a very narrow phone, with the `title` preserving the meaning. The earlier failure mode to avoid: a two-row cluster — e.g. putting the ⋯ on its own line, or a wrapping pill — gets bottom-aligned to the tall avatar and pushed **up behind the blue cover**, where the buttons vanish. So the `#profile-actions` div is the plain `ml-auto flex items-center` **one-row** cluster (no `flex-wrap`/drop-below), and the pill text never wraps. It has `items-stretch divide-x overflow-hidden rounded-lg ring-1` with **`divide-x` segment dividers** whose colour tracks the ring (`divide-slate-300`, or `divide-emerald-300` when mutual, plus dark variants) so the three segments read as distinct cells instead of one blurred panel, and two **equal-width halves** (`flex-1`) that are **always rendered**, so the control's size barely changes between follow states — the relationship status reads from the text, colour, the **seam glyph** and the dividers. The three segments, always in this order: (1) the **clickable outbound toggle** (`<.follow_button variant="segment">`, brand "Follow" / green "✓ Following"); (2) an `aria-hidden` **seam** `<span>` (fixed `w-7` column, shown at **every** width) whose glyph encodes the direction (`·` no relationship, `→` you follow them, `←` they follow you, `⇄` mutual — the seam, ring and dividers go emerald on a mutual "vernetzt" follow, `title` = gettext("You follow each other")); (3) the read-only **inbound status** `<span>` — green `bg-emerald-700` "✓ `gettext("Follows you")`" when this member follows you, else the muted grey full label "`gettext("Doesn't follow you")`" with **no cross** (the ✗ was dropped: beside the seam glyph it read as part of the seam, so the negative half now leans on its grey colour, the divider and the text; the long German "Folgt dir nicht" can ellipsis-truncate on a very narrow phone, the `title` preserving the meaning). A **mutual** follow lights **both** halves green and the ring emerald, so "vernetzt" is unmistakable at a glance. Emerald is reused from the online-presence dot's "active" language; this is the one place green/✓ is a deliberate status signal beyond the brand/accent/slate palette. Replaces the old collapse-to-bare-toggle treatment (the inbound half + seam no longer disappear when this member doesn't follow back). Route ownership stays in `<.follow_button>`; keep the owner / visitor / logged-in guard on the `:if` at the call site, like `<.follow_button>`. Takes **`live?`** and passes it straight to the `segment` `<.follow_button>`: the profile passes it so the outbound toggle is a `phx-click` handled by `VutuvWeb.UserProfileLive` and the whole pill (counts, seam glyph, both halves) re-renders live with no reload.
- `<.mute_button follow_id={…} muted?={…} />` — the **mute / unmute** toggle, the follower's lever over their *own* feed. Owns the `~p"/follows/:id/mute"` **PUT** route; a muted follow keeps the relationship (and any vernetzt status) but drops the followee's posts out of the muter's feed (`Vutuv.Posts` reads `follows.muted`). A square icon button (heroicons bell-slash) reusing `save_toggle_class(:bookmark, @muted?)` — brand-tint while muted, neutral otherwise. Render only when the viewer follows the member, like `<.follow_button>`'s "Following" state. Used in three places, all driven by `Vutuv.Social.follow_edge/2` (one) or `follow_edges/2` (batched): the **profile header** (`:if={is_binary(@follow_id)}`), the owner's **"Following" list** (`card_list` renders it when the controller passes `muted_by_id`), and the **post card's** non-author ⋯ menu (see `<.post_card>`, so an annoying post is silenced straight from the feed — there it is a `:item` text toggle, "Mute @handle" / "Unmute @handle", not the icon button). Silent and one-directional — unlike a block, which severs everything both ways.
- `<.avatar user={@user} size="xs|sm|md|lg" shape="circle|square" />` — resolves `Vutuv.Avatar`; or pass `src=`. A user **without a picture** renders an **initials tile** (brand-tint `<span>`, initials via the public `name_initials/1`, same module — also used by the shell's top-bar tile); only a call with neither `user` nor `src` gets the neutral SVG placeholder. Add `presence` to overlay the real-time green **online dot**: it wraps the avatar in a `[data-presence-user-id]` span (id from `user.id`, or pass `presence_id` when you only have a `src`) holding a `.presence-dot` the `Presence` JS hook reveals via a generated stylesheet keyed on that id (a CSS attribute-selector rule, so it survives LiveView stream re-renders that would strip a per-node attribute). The dot starts hidden, gated by the member's **`show_online_status?`** setting and the viewer's block list (`ShellLive` tracks online members and pushes each viewer their block-filtered set), so it never shows until the live shell confirms the member is online. Use it on people-listing avatars (user/connection/search/saved lists, post authors, the profile header); the messages page renders its own dots server-side from the same `VutuvWeb.Presence` topic.
- `<.presence_wrap id={…} size="xs|sm|md|lg">…</.presence_wrap>` — the shared **online-dot shell** behind `<.avatar presence>`: wraps any inner content in a `[data-presence-user-id]` span carrying the `.presence-dot` (renders the content **bare** when `id` is nil, so it is safe to wrap unconditionally). Reach for it directly only when the thing that represents a member is **not** a plain `<.avatar>` — e.g. the notifications page wraps both the actor avatar **and** the colored kind-glyph (the stand-in for a picture-less actor, keyed by `actor_id` from `Vutuv.Activity.actor_fields/1`) so the dot rides whichever one shows.
- `<.presence_dot size="xs|sm|md|lg" hook online={…} />` — the green dot itself, the **one** definition of its colour/ring/size/position. Each mode owns its visibility so it can never render an always-on dot: `presence_wrap` renders it with `hook` (adds `.presence-dot`, hidden until the JS hook's generated stylesheet reveals it by `data-presence-user-id`); the two **server-driven** dots (the shell's own top-bar avatar, the messages sidebar) pass `online={…}` from their own state and the dot renders only when true. With neither it renders nothing. Don't hand-write the emerald dot span; render this.
- `<.chip>` — tag chip; pass `navigate`/`href` to render it as a link.
- `<.tag_vote user={…} user_tag={…} viewer={…} />` — the profile **Tags** chip: a brand-tint tag chip whose name links to the tag page, with the visible-endorsement count as a **calm brand-tint pill** right after the name (`bg-brand-100`/`text-brand-700`, dark `bg-brand-800`/`text-brand-100`; recoloured off the coral accent on purpose, a vouch count is social proof, not an alert). For the **read-only** viewer (owner / logged-out) the pill is a plain `<span>`, hidden at 0. For the **actionable** viewer (logged-in non-owner) the **same pill is the endorse toggle**: it is a `<button>` (carrying `data-tag-vote-count`) inside the CSRF `<.vote_form>`, looking identical to the read-only pill until you endorse, then **filling in** (`data-[endorsed=true]:bg-brand-600`/`text-white`); a **zero-count tag shows a `+`** so there is something to click. No more caret + floating corner badge (that split is gone, along with `<.tag_count_badge>` and `caret_up`). Hovering or focusing the chip raises a small **voter roster popover** (`group-hover`/`group-focus-within`, pure CSS, no JS, no library): each endorser is a **link to their profile** (the card takes pointer events and an invisible `after:` strip bridges the `mb-2` gap so the cursor can reach it), names **truncate**, and an "and N more" link to this member's per-tag endorser list (`/:slug/tags/:tag/endorsers`, a public paginated people list, a `UserTagController` action served in HTML and the agent formats, listing everyone who endorses this member for the tag) closes out the capped list (~6). The actionable viewer's **own row is pre-rendered** in the roster (hidden, marked `data-self-endorser`, and excluded from the server list so it never doubles); the `TagVote` JS reveals it the moment they endorse, so they appear in the roster **without a reload**. For them the popover is rendered even on a zero-roster tag, but hover is disabled (`active?` toggles the `group-hover`/`group-focus-within` classes, which the JS turns on once there is a visible row) so an empty card never pops. The toggle is a CSRF `<.form>` (the no-JS fallback) that the `TagVote` enhancement in `app.js` drives over fetch (POST endorse / DELETE undo), flipping `data-endorsed`, swapping the `+`↔count text and revealing/hiding the self row. The count is the **visible** endorsement tally (`compact_count`, the issue #783 gate); the roster comes from the profile's `UserTagEndorsement.visible_with_endorser/0` preload (endorsers loaded with the endorsement, sorted newest-first by their UUID v7 id, so it is in-memory, no per-tag query). The controller (`UserTagEndorsementController`) answers the fetch request with `{count, endorsed}` JSON and a plain submit with the classic flash + redirect. **`live?`** (default false): on the profile (a LiveView, `VutuvWeb.UserProfileLive`) the chip is passed `live?`, so the pill is a `phx-click="endorse"`/`"unendorse"` button (`phx-value-id` the user_tag id) and the LiveView re-renders the pill + roster server-side on every toggle — no fetch, no `TagVote` JS, no reload (the count, the `+`↔count text and the self-row in the roster all come from the re-render). The CSRF `<.vote_form>` + `TagVote` enhancement stays the path for any non-live caller.
- `<.secret_once flash={@flash} key={:new_token} label={…} class="mt-4" />` — the **show-once credential reveal** (access tokens, OAuth client secrets, webhook signing secrets): a brand-tint box with a `select-all` `<code>` line, rendered only while the one-shot flash under `key` holds the freshly minted secret (`data-secret-once` hook). Every "copy it now, it will not be shown again" box goes through this; don't hand-roll a fourth.
- `<.count_badge count={n} class="…" />` — coral unread badge; renders nothing when 0; `class` positions it. Add `ring-2 ring-white dark:ring-slate-900` via `class` when it overlaps an icon/avatar (the shell does this).
- `<.local_time at={dt} id={…} format={…} class={…} />` — the **one** rendering of a **viewer-timezone** timestamp (`<time data-localtime>`) rewritten client-side. Stored times are UTC, so it always emits the `datetime`/`title` as unambiguous ISO-8601 with a trailing `Z` (naive datetimes treated as UTC, `DateTime`s via their own offset), the form the `LocalTime` pass converts from — hand-rolling a space-separated stamp made some browsers read it as *local* time. Pass `id` inside a LiveView so the `LocalTime` hook attaches (it needs a DOM id); omit it on classic pages, where `app.js`'s `time[data-localtime]` sweep handles it. `format` is the `Calendar.strftime/2` fallback shown with JS off (default `"%Y-%m-%d %H:%M"`). Used by the messages thread, the tag-endorsers list and the admin ad pages — **don't hand-roll a `<time>` for a UTC stamp; render this** (or `<.post_time>` for posts).
- `<.post_time at={dt} id={…} class={…} />` — the **post** timestamp, rendered **server-side in Europe/Berlin time** (`Vutuv.BerlinTime`, the site's canonical German clock — the same calendar day the age display and daily ad rotation use), with **no** client rewrite. A post from **today** shows just the time (German "08:42 Uhr", a bare "8:42 AM" under other locales); older posts the full short date and time ("02.07.26, 08:42"). The `<time>` keeps a UTC `datetime` for machines/agents and a full-date `title` for hover, but the visible text is final from the server, so it deliberately carries **no** `data-localtime` marker (the JS localizer skips it). Used by the post card and the thread/notification post preview (`VutuvWeb.PostComponents`). This is the deliberate exception to `<.local_time>`'s viewer-timezone rule: post times are Berlin, everything else is the viewer's zone.
- `<.frozen_banner class="…">{text}</.frozen_banner>` — the owner-facing **moderation freezer notice** (amber strip, ⚑ glyph, trailing "Review" link to `/moderation/cases`, `data-frozen-banner` hook). The one rendering of "only you can see this while a report is handled"; the post card passes `class="mb-3 rounded-lg px-3 py-2 text-xs"`, the profile header `class="rounded-2xl px-4 py-3 text-sm"`. Guard with `:if={owner and frozen}` at the call site. Amber (`amber-50/800`, dark `amber-900/30`/`amber-200`) is reserved for moderation notices.
- `compact_count/1` — **every rendered count goes through this** (plain function, same module): exact up to 999, above that floored to `1K` / `80K` / `5M`. `<.count_badge>` applies it already; call it yourself for inline numbers (follower counts, "Load N of M more"). Page-number links in `<.pager>` are navigation, not counts — they stay exact.
- `highlight/2` — wraps every case-insensitive occurrence of the needle(s) in a text in a brand-tinted `<mark>` (`bg-brand-100 text-brand-900`, dark `bg-brand-500/30 text-brand-100`): the search result match marker (people names, tag chips, post excerpts on `/search`). Plain function, same module; returns safe HTML from escaped parts. Phonetic ("similar names") results deliberately get no marker — the absent highlight is part of the exact-vs-similar distinction.
- `delimited_count/1` — the **exact**, thousands-grouped form (`60123` → `"60,123"`, German `"60.123"`), the deliberate exception to `compact_count/1` for the **landing-page member counter**, which shows the precise total and ticks it up live. Rendered by the embedded `VutuvWeb.MemberCountLive` pill (fed by `Vutuv.Accounts.MemberCounter`), not the controller.
- `<.page_header title={…} crumbs={[…]} />` — **legacy Track 1** page top: the `.profile-header > .profile-header__info > h1` block (rendered when `title` is set) and/or the `.breadcrumbs` row wrapping `gen_breadcrumbs/1` (rendered when `crumbs` is set). `title` is a plain string the call site already builds (e.g. `gettext("Emails belonging to ") <> full_name(@user)` or `gettext("Tags of %{name}", name: full_name(@user))`); `crumbs` is the list you used to hand to `gen_breadcrumbs/1` (`gen_breadcrumbs/1` is called fully qualified inside the component because `VutuvWeb.UI` doesn't import `UserHelpers`). new/edit pages pass only `crumbs`. Replaces the ~47 header + ~65 breadcrumb boilerplate blocks across the controller pages. Pages whose header carries more than the single h1 (avatar, buttons, …) keep their hand-written markup; `user/show.html.heex`'s bespoke profile header is excluded.
- `<.form_error changeset={@changeset} />` — **legacy Track 1** changeset-error banner (`.alert.alert-danger` + `.editform__error`, styled by `components.css`, not utilities); renders only when `@changeset.action` is set. Shared by the `editform` `form_content` templates so the banner is written once.
- `<.form_actions backlink={@backlink} delete_to={assigns[:delete_to]} confirm=… />` — **legacy Track 1** Cancel/Submit actions row (`.editform__actions` with a `.button.button--cancel` Cancel link to `@backlink` and a `.button` Submit). Shared by the `editform` `form_content` templates. On **edit** forms, `delete_to` appends the canonical delete control (`id="delete-entry"`, a `.button--danger` link, CSRF-protected DELETE behind `data-confirm`; `confirm` overrides the default "Are you sure?"). The shared `form_content` templates thread it through as `delete_to={assigns[:delete_to]}` and only the `edit.html.heex` wrappers pass it, so new-forms stay delete-free — `test/vutuv_web/controllers/profile_edit_affordances_test.exs` enforces both directions. Forms with a custom submit label or no Cancel keep their own hand-written row.
- `<.edit_delete_actions edit_to=… delete_to=… show_to=… confirm=… class=… />` — **legacy** icon-button group: the `.btns-right` wrapper + `.button.button--icon.button--small` CSS-glyph icons (`i.icon.icon--edit|--delete|--search`), view → edit → delete order, delete via the `delete` method (CSRF) + `button--danger`. **Now used only by the admin/group index tables** — the user-facing profile management lists/shows switched to the calm `<.row_actions>` (see above). Each `*_to` is optional; `title_*` set tooltips, `:extra` adds a trailing button. Keep the owner/admin guard at the call site.
- `<.card_section empty={…} add_href={…} add_label={…} empty_text={…}>…</.card_section>` — **legacy Track 1** card shell (`<div class="card-list"><section class="card">…</section></div>`, styled by `components.css`). Under the unified card UX its "Add" is the dashed `<.empty_add>` tile (label from `add_label`) rendered **above** the content (compose-on-top), the same way whether the card is empty or populated (the old bottom `.card__morelink` and the header add button are both gone). A visitor (no `add_href`) on an empty card still gets the plain `<p class="card__empty">` line (`empty_text`, default gettext("Nothing here yet.")). New/edit form wrappers pass no `add_href`, so they just wrap the inner block. Used by the email/phone_number/url/social_media_account/work_experience/address/user_tag/group/search_term indexes + the new/edit wrappers.
- `<.pager params={@conn.params} total={@row_count} />` — numbered pagination for offset-paginated **browse** pages (followers, tags, users); the math lives in `Vutuv.Pages` (`paginate/4` on the query, default 250/page). Renders nothing when one page fits. Optional `per_page` overrides the page size (must match the `paginate/4` the query used — the tag-endorsers table passes a smaller one) and optional `query` (a map) is carried onto every page link so pagination keeps the current sort/filter instead of dropping it. **Feed** LiveViews (notifications, /feed posts) paginate with a cursor + "Load more" button appended into the stream instead (`Vutuv.Activity.notifications_page/2`, `Vutuv.Posts.feed_page/2`) — don't mix the two styles.
- `<.load_more :if={@more?} />` — the cursor-pagination "Load more" control those feed-style LiveViews share: a centered secondary `<.button id="load-more">` that emits the `"load-more"` event. The inner block overrides the default label (notifications shows the remaining count); `class` adds wrapper utilities (e.g. `mt-6`).
- `<.icon_repost class=… />` / `<.icon_reply class=… />` / `<.icon_bookmark filled?={…} class=… />` / `<.icon_heart filled?={…} class=… />` — the Track 2 outline SVG icons used in more than one module (post card "Reposted by"/"Replying to" lines + action bar; shell + action bar; the Like toggle on the post action bar **and** the profile's "like this member" control). Size via `class` (defaults `h-5 w-5`); `filled?` switches bookmark/heart to solid. Icons used by a single LiveView stay private to it by convention.
- `<.user_bookmark_button saved?={…} target_id={…} />` / `<.user_like_button saved?={…} target_id={…} />` — the profile header's **private save** toggles (bookmark / like a *member*, the people-equivalent of the post action bar's bookmark/like). Square icon buttons sized to sit beside the header's text controls; they own the `~p"/user_bookmarks…"` / `~p"/user_likes…"` route shapes (POST to save, CSRF DELETE to remove, branched on `saved?`) and the solid icon marks the saved state. The save is silent and private (no follow/connection, no notification, no public count). Keep the logged-in / not-owner / not-blocked guard on the `:if` at the call site, like `<.follow_button>` / `<.mute_button>`.
- `<.post_card post={…} viewer={…} mode={:preview|:full} surface={:card|:flat}>` — **Track 2** post card, the one rendering of a post everywhere (feed + profile "Posts" section use `:preview`, the permalink page `:full`). `surface={:card}` (default) stands alone; `surface={:flat}` renders without the card shell for embedding inside an existing card — the profile's Posts section stacks flat posts in a `divide-y divide-slate-100 dark:divide-slate-800` list with `py-4 first:pt-0 last:pb-0` rows, under a `<.section_header>` whose owner `add_href` points at the `/feed` composer. In that **threaded** profile list a reply is nested under the post it answers: the parent (from the preloaded `reply_ref`, `Posts.reply_ref_state/1` → `{:parent, parent}`) renders above as a compact context row (`VutuvWeb.UserHTML.reply_parent_preview/1` — linked avatar/name + one-line excerpt linking to the parent) joined by the `border-l-2` rail + a `top-[1.125rem]` tick to the reply's avatar centre, and the reply card is passed **`show_reply_banner={false}`** so its own "Replying to @handle" banner is dropped (the inline parent shows the relationship once); top-level posts and degraded replies render standalone with the banner intact. Author header (avatar/name/time→permalink, "edited" hint, generic 🔒 for restricted posts — never the deny list), markdown body in `.markdown` (preview adds `line-clamp-5` + server-side block-boundary cut with a "Read more" link), preview thumbnail row vs full gallery (inline-referenced attachments render in place, the rest below), tag chips. When `viewer` is the author, the card renders its own top-right `<.card_menu id={"post-menu-#{post.id}"}>` with Edit and a confirmed red Delete; everyone else gets the quiet ⋯ menu with **Report** plus, when `viewer_follow` (the viewer's `%{id:, muted?:}` edge to the author, passed in) is set, a **Mute / Unmute @handle** toggle (PUT `/follows/:id/mute`) — so an annoying post is silenced straight from the feed. The feed attaches `viewer_follow` per entry (batched `Social.follow_edges/2`), the permalink per post; lists that don't pass it simply show no mute item. No slot — every rendering of a post carries the menu. Lives in `VutuvWeb.PostComponents` and is **not** globally imported — `import VutuvWeb.PostComponents` at the call site.
- **Markdown body recipe:** wrap rendered Markdown in `<div class="markdown text-slate-800 dark:text-slate-200">`. `.markdown` (in `components.css`, light + dark) carries only structure — paragraph/list/blockquote/code spacing, `overflow-wrap: break-word` on the wrapper (inherited, so a long unbreakable token — a pasted URL, a long word — wraps instead of overflowing the column and scrolling a phone sideways; `pre` keeps `white-space:pre` + `overflow-x:auto` and still scrolls), tables (`display:block` + `overflow-x:auto` so a wide table scrolls inside the card, hairline row borders; used by the `/developers` docs) and `img.post-inline-image` — text color comes from the wrapper's utilities. **User-written** Markdown (posts) renders through `VutuvWeb.Markdown.render_post/2` (inline images restricted to own attachments) / `render_preview/2`, never raw Earmark; repo-authored trusted content (`priv/dev_docs` via `DevDocController`) may use Earmark directly. The renderer also turns every **`@handle`** of an existing member into a same-tab profile link (`<a class="mention" href="/handle" title="Full Name">@handle</a>`) — resolved against the DB in one batched query per body, skipping handles inside `code`/`pre`/existing links, so it works in posts, chat, ads and the RSS/JSON renderings alike. It likewise turns every **`#hashtag`** into a link to that tag's `/tags/:slug` page (`<a class="hashtag" href="/tags/slug">#hashtag</a>`) — but **only** when the tag exists and has at least one visible member (`Vutuv.Tags.linkable_slugs/1`, the same gate the tag page lists by), so a link never lands on an empty tag page; an unknown or empty tag stays plain text. `.mention` and `.hashtag` (in `components.css`, not `.markdown`-scoped since chat bubbles aren't wrapped) only set `font-weight: 600`; the colour is inherited from the surrounding link so it adapts to prose vs message bubbles and needs no dark counterpart. A mention's `title` carries the member's name as a native hover tooltip.

### Shell & layout facts (don't re-implement)

- The chrome — sticky top bar + mobile bottom tab bar with the live unread badges — is `VutuvWeb.ShellLive`, embedded in `app.html.heex`. Pages render **inside** it; never add their own nav.
- **Flash = top-right toasts** (`#toast-tray` in `app.html.heex`). Never add inline flash banners; `VutuvWeb.LayoutHTML.flash/1` and its empty partial were deleted along with the last inline calls.
- In-app real-time events go through `Vutuv.Activity` (PubSub on `"user:<id>"`) and `VutuvWeb.Presence`.
- **Gettext locale must be set per process.** `VutuvWeb.Plug.Locale` resolves it per
  request and stores it in the session; LiveViews re-apply it on mount via
  `VutuvWeb.LiveLocale` (called from `Live.InitAssigns` and `ShellLive.mount`).
  A new LiveView mounted outside the `live_session` must call it too, or its copy
  (and the whole shared chrome) silently falls back to English.
- **Legacy page anatomy:** `.profile-header` h1 → `.breadcrumbs` → `.card-list` >
  `.card` — the header + breadcrumbs are emitted by `<.page_header title=… crumbs=… />`
  (new/edit pages pass only `crumbs`); the owner "Add" is the dashed `<.empty_add>`
  tile that `<.card_section add_href=…>` renders above the list (empty *or* populated),
  per-entry edit/delete is the calm `<.row_actions>` (placed in a `td.text-right` for
  table rows), and a visitor's empty collection renders `<p class="card__empty">`.
  Copy this from `email/index`. The admin/group index tables still use the legacy
  `<.edit_delete_actions>` icon group.
- Error pages (`VutuvWeb.ErrorHTML`) render the `.error-page` card (code, message,
  "Back to the start page") and must work with and without the app layout.
  `.error-page__note` is the prominent brand-tinted callout inside that card
  (the `/username` placeholder helper uses it to own up to a broken newsletter
  link); it is the one non-muted line, distinct from the grey `.error-page__hint`.

### CSS architecture (don't break it)

`app.css` order: `@import "tailwindcss"` → `@import "./components.css" layer(components)`,
**at the top** before `@source`/`@theme` — the `@import … layer()` only works there
(CSS ignores `@import` after other rules). `components.css` lives in the `components`
layer, so it beats Preflight (base) but loses to Tailwind utilities, which lets
hand-written pages use utilities freely. There is **no `legacy.css` anymore** — do not
reintroduce it (a regression test enforces this). Dev serves `/assets/app.css`
undigested, so hard-reload (Cmd+Shift+R) after a rebuild. **Dev does not use
`tailwind --watch`:** the v4 CLI's watch mode rebuilds from a cached copy of
`@import`ed CSS and ignores CSS edits outright (verified on 4.0.0 and 4.3.0), so
`VutuvWeb.TailwindWatcher` (wired as the dev watcher) runs a correct one-shot
build on every CSS/template/JS change instead. Edits to `components.css` reach
the browser within ~2s; if they ever don't, suspect that watcher first.

### Don'ts

- No theme toggle (dark = system).
- Don't reskin legacy pages per-template — edit `components.css`.
- Don't invent new bespoke colours; stay on brand / accent / slate.
- No em-dashes in UI copy (project-wide rule).
